'use strict'

class TreinamentoController {
    async index({ }) {
        const { page } = request.get()
    }
    async store({ }) {

    }
    async show({ }) {

    }
    async update({ }) {

    }
}

module.exports = TreinamentoController
